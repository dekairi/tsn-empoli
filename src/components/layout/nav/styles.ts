import styled from 'styled-components';
import { Ul } from '../../styled';

export const NavList = styled(Ul)`
  display: flex;
  
  li {
    margin-left: 30px;
  }
  
  a {
    min-width: auto;
  }
`;
